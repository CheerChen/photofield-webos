# 更新日志

本文档记录项目的重要变更。英文版本见 [CHANGELOG.md](CHANGELOG.md)。

## [Unreleased]

### 新增

- 设置页支持输入完整 IPv4 地址。
- 大型照片网格支持红键上一页、黄键下一页。
- 更新应用图标和启动图，并保留用于重新生成的 SVG 源文件。
- Kiosk 开始播放时显示简短的 Lofi 操作提示。

### 调整

- 统一 Kiosk 的启动和恢复入口，确保各播放入口使用相同的 PIN 校验和播放记录规则。
- 扫描未发现可连接实例时保留已缓存的照片源；没有可用照片源时显示可执行的提示。
- 缓存近期照片元数据、延后创建 scene，并在 Kiosk 淡入前预加载图片。
- 源和相册名称使用文本节点渲染，不再作为 HTML 解析。
- 图片加载期间继续导航时，查看器会显示最后一次请求的照片。
- 严重播放错误与常规提示使用独立提示区域。

## [0.3.0] - 2026-08-01

### 新增

- 扫描已配置服务器的 `8000–8010` 端口，自动发现 Photofield 实例；支持缓存结果和手动重新扫描。
- Kiosk 新增相片填充模式：竖图氛围、完整显示和裁切填满。
- Kiosk 新增随机播放和顺序播放设置。
- 新增彩键 Lofi 播放列表、曲目切换和播放状态提示。
- Kiosk 播放期间抑制 webOS 屏保。

### 调整

- 改进源统计的刷新时机和连接失败提示。
- 按目标显示尺寸选择图片变体。
- 并行获取幻灯片相册计数，并重试短暂的 Photofield 服务端错误。
- 服务端连续出错后停止播放，避免无限重试。
- 不再将生成的 IPK 包纳入版本控制。

## [0.2.0] - 2026-07-31

### 新增

- 相册选择页改为四列封面卡片。
- 新增内联 SVG 图标，解决 webOS 环境中 Unicode 图标缺字的问题。
- 将 Wallpaper 源的子目录展开为独立相册。

### 调整

- 顺序加载相册封面，降低服务器负载。
- 修正固定高度相册网格的尺寸计算，避免封面被裁切。

## [0.1.2] - 2026-07-31

### 调整

- Kiosk 播放控制从不稳定的长按 OK 改为媒体键。
- Kiosk 支持播放、暂停、快退、快进和停止键。
- 暂时移除 X 源的 PIN 锁及对应设置项。

## [0.1.1] - 2026-07-31

### 调整

- 照片网格改用 FLEX 布局，提高显示密度。
- 网格预览优先使用预生成的图片变体。

## [0.1.0] - 2026-07-31

### 新增

- 初始 webOS 相册客户端，提供源选择、相册浏览、照片网格、查看器和 Kiosk 幻灯片模式。
- 提供 Photofield 适配层、遥控器导航、持久化设置、PIN 支持、测试和 IPK 打包脚本。
