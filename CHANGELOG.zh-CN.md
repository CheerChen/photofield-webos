# 更新日志

本文档记录项目的重要变更。英文版本见 [CHANGELOG.md](CHANGELOG.md)。

## [0.6.0] - 2026-08-06

### 新增

- Kiosk Lofi 音乐新增在线电台来源：13 个精选公共 MP3 电台直播流，按四色主题分组，彩键选色、上/下键切台，操作与本机曲目一致。
- 设置新增「Lofi 来源」（本机曲目 / 在线电台）；悬浮音乐指示器新增连接中状态：均衡器脉动、站名后动画省略号。
- 新增运行时语言切换（设置页入口），全部界面文案抽取到 i18n 模块（英文 / 简体中文）。
- README 新增截图。

### 调整

- 电台模式在声音真正开始流动时淡入，手动切台/换色前先淡出；本机曲目、挂起与停止仍为立即切换。
- 电台恢复播放时重连直播边缘，不再回退到挂起前的进度。
- 移除 esbuild 打包，改用浏览器原生 ES modules，打包不再需要构建步骤。

### 修复

- 电台流打不开、播放中报错或静默挂起六秒时自动换下一个台，不再让 Kiosk 无声卡住；整个颜色池都失败才放弃。
- 抑制 FLEX 布局的日期/地点文本头，网格行不再出现左倒缩进；不再采用带此类头部的 Web UI 场景。
- 收紧设置弹层间距，1080p 屏幕不再溢出。

### 移除

- 移除启动行为设置（始终从上次播放位置继续）。

## [0.5.0] - 2026-08-05

### 新增

- 源选择页改为照片拼贴卡片，并增加轻量氛围背景和实时扫描状态。
- Kiosk 新增时钟与日期、四种信息显示模式、EXIF 地点反向解析、音乐均衡器提示，以及封面模式照片的四向 Ken Burns 动效。
- 设置页按用途分组，并增加相册排序、仅图片或图片和视频范围，以及更明确的选项说明。
- 新增 Playwright 行为测试，覆盖启动、相册与网格导航、查看器、Kiosk 和完整返回栈。
- 新增 ESLint、自动单元测试发现脚本和手动触发的 GitHub Actions 验证流程。

### 调整

- 使用面向 webOS Chrome 53 运行时的单一 esbuild bundle，替代手工维护的脚本加载顺序。
- 全屏页面和浮层统一使用栈式导航，并统一按键激活与返回行为。
- 将媒体清理、预览候选选择、LRU 缓存和可取消异步 generation 守卫整理为共享核心模块。
- 平台适配、打包和 CDP 工具改由 `webos-tv-kit` 子模块统一提供。
- 网格改用合成层滚动和更大的持久化缩略图，减少浏览卡顿。

### 修复

- 从浏览或播放页面返回时，同步更新照片源扫描进度和相册数量。
- 取消操作后，过期的查看器、幻灯片、封面、音乐和导航回调不再更新界面。
- 在幻灯片切换、解码失败回退以及视频与音乐切换时，确定性释放音视频资源。

## [0.4.0] - 2026-08-02

### 新增

- 查看器与 Kiosk 支持视频播放：视频以原始文件有声播放，浏览器无法解码时回退到已加载的预览图，播放期间 Lofi 音乐暂停，切到下一张照片后恢复。
- 源选择页红键对该源触发文件系统重扫：扫描中卡片置灰、禁止进入与播放，完成后刷新相册数量并清理 scene 缓存。
- 被动忙时检测：即便扫描由 Photofield 网页端触发，扫描中的源也会置灰并拦截进入与播放，直到相关索引任务结束。
- 设置页支持输入完整 IPv4 地址。
- 更新应用图标和启动图，并保留用于重新生成的 SVG 源文件。
- Kiosk 开始播放时显示简短的 Lofi 操作提示。
- 新增进入 Kiosk 自动播放 Lofi 的开关（默认开启），并支持按颜色随机循环播放。
- 将每种颜色的 Lofi 曲单扩展为 7 首，并重新按「晨光」「草木」「金色时刻」「夜空」调整分组。

### 调整

- 网格、查看器和 Kiosk 的图片加载统一走有序回退链：预生成变体缺失时依次回退到其他变体或服务端动态预览，不再直接导致该张幻灯片失败。
- 对浏览器可直接解码且在单个显示面解码尺寸预算内的照片直接使用原图，跳过变体请求；GIF 动图不纳入原图直出。
- 统一 Kiosk 的启动和恢复入口，确保各播放入口使用相同的 PIN 校验和播放记录规则。
- 扫描未发现可连接实例时保留已缓存的照片源；没有可用照片源时显示可执行的提示。
- 缓存近期照片元数据、延后创建 scene，并在 Kiosk 淡入前预加载图片。
- 源和相册名称使用文本节点渲染，不再作为 HTML 解析。
- 图片加载期间继续导航时，查看器会显示最后一次请求的照片。
- 严重播放错误与常规提示使用独立提示区域。

## [0.3.0] - 2026-08-01

### 新增

- 扫描已配置服务器的 `8000–8010` 端口，自动发现 Photofield 实例；支持缓存结果和手动重新扫描。
- Kiosk 新增相片填充模式：竖图氛围、完整显示和裁切填满。
- Kiosk 新增随机播放和顺序播放设置。
- 新增彩键 Lofi 播放列表、曲目切换和播放状态提示。
- Kiosk 播放期间抑制 webOS 屏保。

### 调整

- 改进源统计的刷新时机和连接失败提示。
- 按目标显示尺寸选择图片变体。
- 并行获取幻灯片相册计数，并重试短暂的 Photofield 服务端错误。
- 服务端连续出错后停止播放，避免无限重试。
- 不再将生成的 IPK 包纳入版本控制。

## [0.2.0] - 2026-07-31

### 新增

- 相册选择页改为四列封面卡片。
- 新增内联 SVG 图标，解决 webOS 环境中 Unicode 图标缺字的问题。
- 将 Wallpaper 源的子目录展开为独立相册。

### 调整

- 顺序加载相册封面，降低服务器负载。
- 修正固定高度相册网格的尺寸计算，避免封面被裁切。

## [0.1.2] - 2026-07-31

### 调整

- Kiosk 播放控制从不稳定的长按 OK 改为媒体键。
- Kiosk 支持播放、暂停、快退、快进和停止键。
- 暂时移除 X 源的 PIN 锁及对应设置项。

## [0.1.1] - 2026-07-31

### 调整

- 照片网格改用 FLEX 布局，提高显示密度。
- 网格预览优先使用预生成的图片变体。

## [0.1.0] - 2026-07-31

### 新增

- 初始 webOS 相册客户端，提供源选择、相册浏览、照片网格、查看器和 Kiosk 幻灯片模式。
- 提供 Photofield 适配层、遥控器导航、持久化设置、PIN 支持、测试和 IPK 打包脚本。
